## Trigger Rules

Main routing remains `{{canonicalPlaybook}}`. Gemini does not use the Codex / Claude plugin marketplace in this template; keep this file as a thin pointer to the same canonical playbook to avoid drift.
